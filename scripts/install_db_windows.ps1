# Opcional: usado apenas se você quiser executar SQL manualmente via psql.
# O app cria as tabelas sozinho no startup.
Write-Host "O app cria tabelas e seed automaticamente. Use database/00_all.sql apenas se quiser aplicar manualmente no PostgreSQL."
